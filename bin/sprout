#!/usr/bin/env node

require('coffee-script/register');
var CLI = require('../lib/cli'),
    cli = new CLI();

cli.emitter.on('data', function(data){
  console.log(data.green);
});

cli.run(process.argv.slice(2)).done();
